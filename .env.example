# Required for AI hook generation and slideshow authoring.
# Without it the app still ingests, analyses and charts your data, and falls
# back to evidence-only templates for generation.
ANTHROPIC_API_KEY=sk-ant-...
